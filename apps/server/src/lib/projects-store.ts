/** Transitional: the projects store moved to `src/projects/`; this keeps the orchestrator's imports working. */
export { ProjectError, summarizeProject } from "../projects/store.ts";
export type { ProjectsStore } from "../projects/store.ts";
