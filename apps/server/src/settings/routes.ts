import type { FastifyInstance } from "fastify";
import type { AppContext } from "../context.ts";

/** `/api/settings`: ported from apps/web/src/app/api/settings. */
export function registerSettingsRoutes(_app: FastifyInstance, _ctx: AppContext): void {}
