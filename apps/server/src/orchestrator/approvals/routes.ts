/** `/api/portal/approvals/**`. STUB until phase 2 step 5. */
import type { FastifyInstance } from "fastify";
import type { AppContext } from "../../context.ts";

export function registerApprovalRoutes(_app: FastifyInstance, _ctx: AppContext): void {}
