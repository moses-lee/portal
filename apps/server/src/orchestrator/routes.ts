import type { FastifyInstance } from "fastify";
import type { AppContext } from "../context.ts";

/** `/api/portal/**`: ported from apps/web/src/app/api/portal. */
export function registerOrchestratorRoutes(_app: FastifyInstance, _ctx: AppContext): void {}
