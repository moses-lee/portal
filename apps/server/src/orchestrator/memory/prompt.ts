/**
 * What the system prompt says about this domain's tools and rules: a few short lines, appended to
 * the base prompt of every turn by `prompt.ts`. Empty until the domain lands.
 */
export const guidance = "";
