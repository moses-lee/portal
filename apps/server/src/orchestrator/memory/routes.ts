/** `/api/portal/memory/**` (entities, records, inbox, revisions, core). STUB until phase 2 step 4. */
import type { FastifyInstance } from "fastify";
import type { AppContext } from "../../context.ts";

export function registerMemoryRoutes(_app: FastifyInstance, _ctx: AppContext): void {}
