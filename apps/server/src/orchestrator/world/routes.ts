/** `/api/portal/world/**`. STUB until phase 2 step 3. */
import type { FastifyInstance } from "fastify";
import type { AppContext } from "../../context.ts";

export function registerWorldRoutes(_app: FastifyInstance, _ctx: AppContext): void {}
