/** `/api/portal/jobs/**`, `/api/portal/runs/**`, `/api/portal/intents/**`. STUB until phase 2 step 2. */
import type { FastifyInstance } from "fastify";
import type { AppContext } from "../../context.ts";

export function registerJobRoutes(_app: FastifyInstance, _ctx: AppContext): void {}
